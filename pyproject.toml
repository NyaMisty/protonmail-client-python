[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "protonmail-client-python"
version = "0.1.0"
description = "ProtonMail client utilities and login helpers"
requires-python = ">=3.8"
dependencies = [
    "bcrypt>=4.0.1",
    "requests>=2.31.0",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
]

[project.optional-dependencies]
pgp = ["PGPy>=0.6.0", "standard-imghdr>=3.13.0"]
verify = ["proton-client", "python-gnupg>=0.5.0"]
captcha = [
    "brotli",
    "httpx[http2]",
    "numpy",
    "opencv-python",
    "pytweening",
    "zstandard",
]

[project.scripts]
protonmail-client = "protonmail_client.cli:main"
proton-captcha-solve = "protonmail_client.captcha_solver_cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
